// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.coingecko.com'], // Adiciona o domínio permitido
  },
};

export default nextConfig;
